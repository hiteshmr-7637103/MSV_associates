{
  "name": "msv-associates-workflow",
  "version": "1.0.0",
  "description": "Insurance Policy Workflow Management System for MSV Associates",
  "main": "index.js",
  "scripts": {
    "install-all": "npm.cmd install && npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "server": "npm.cmd start --prefix backend",
    "client": "npm.cmd run dev --prefix frontend",
    "dev": "concurrently \"npm.cmd run server\" \"npm.cmd run client\"",
    "build": "npm.cmd run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
